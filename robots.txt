# ┌──────────────────────────────────────────────────────────────────────┐
# │  PRE-LAUNCH STATE - THIS FILE BLOCKS ALL SEARCH ENGINES              │
# │                                                                      │
# │  The site is deployed to an unlisted preview URL and must not be     │
# │  indexed yet. Run ./go-live.sh to flip this file (and the noindex    │
# │  header in netlify.toml) when you are ready to launch.               │
# │                                                                      │
# │  If this ships to the real domain, Morgan's name will never be       │
# │  indexed and the site's single biggest SEO goal fails silently.      │
# └──────────────────────────────────────────────────────────────────────┘

User-agent: *
Disallow: /

# --- The lines below take effect at launch (go-live.sh swaps them in) ---
# User-agent: *
# Allow: /
# Disallow: /thanks.html
# Sitemap: https://brandywinedpc.com/sitemap.xml
